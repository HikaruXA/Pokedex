<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/header") %>
  </head>
  <body class="bg-gray-100 text-gray-800">
    <div
      class="container mx-auto p-5 flex justify-center items-center min-h-screen"
    >
      <div
        class="relative bg-red-600 w-80 md:w-96 p-5 rounded-lg shadow-lg border-4 border-black"
      >
        <div
          class="absolute top-2 left-2 w-8 h-8 bg-white border-2 border-black rounded-full"
        ></div>
        <div
          class="absolute top-4 left-12 w-4 h-4 bg-blue-400 border border-black rounded-full"
        ></div>
        <div
          class="absolute top-4 left-18 w-4 h-4 bg-green-400 border border-black rounded-full"
        ></div>
        <div
          class="absolute top-4 left-24 w-4 h-4 bg-yellow-400 border border-black rounded-full"
        ></div>

        <h1 class="text-2xl font-bold text-center text-white uppercase mb-3">
          <%= pokemon.name %>
        </h1>

        <div
          class="bg-gray-300 border-4 border-black p-3 rounded-lg flex justify-center"
        >
          <img
            src="<%= pokemon.sprites.front_default %>"
            alt="<%= pokemon.name %>"
            class="w-32 h-32"
          />
        </div>

        <div class="mt-4 text-white text-center space-y-2">
          <p class="text-lg font-semibold">Height: <%= pokemon.height %></p>
          <p class="text-lg font-semibold">Weight: <%= pokemon.weight %></p>
          <p class="text-lg font-semibold">
            Type: <%= pokemon.types.map(t => t.type.name).join(', ') %>
          </p>
        </div>

        <div class="text-center mt-5">
          <a
            href="/"
            class="px-4 py-2 bg-blue-500 text-white font-bold rounded-lg shadow-lg hover:bg-blue-600 transition"
          >
            Back
          </a>
        </div>
      </div>
    </div>
    <%- include("../partials/footer") %>
  </body>
</html>
